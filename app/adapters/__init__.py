"""Adapters layer.

This layer contains the entry points and integrations with external
systems. Adapters translate between the application and external
concerns like HTTP, databases, and external services.

Structure:
- api/: FastAPI routers and endpoints
- dependencies/: Dependency injection helpers
- middlewares/: HTTP middleware
"""
